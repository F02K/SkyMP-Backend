# Neutral data seed

Copy this directory to the persistent `DATA_DIR` for a new project. Existing installations should migrate their own files with `npm run migrate:data`; do not replace live data with these empty templates.
